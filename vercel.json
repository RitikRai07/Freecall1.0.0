{
  "buildCommand": "npm run build",
  "installCommand": "npm install && npm --prefix ./freecall install",
  "outputDirectory": "freecall/dist",
  "routes": [
    {
      "src": "^/(?!.*\\.).*$",
      "dest": "/index.html"
    }
  ]
}
