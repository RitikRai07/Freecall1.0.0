{
  "name": "meet-root",
  "private": true,
  "scripts": {
    "dev": "npm --prefix ./freecall run dev",
    "build": "npm --prefix ./freecall install && npm --prefix ./freecall run build",
    "preview": "npm --prefix ./freecall run preview"
  },
  "engines": {
    "node": "22.x"
  }
}
